{
  "name": "prawin",
  "version": "1.0.0",
  "description": "",
  "main": "main.js",
  "files": [
    "side_style.css",
    "*.css",
    "*.png",
    "*.js"
  ],
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "electron .",
    "build": "electron-builder build --mac --win --publish never",
    "deploy": "electron-builder build --mac --win --publish always"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "electron": "^12.0.2",
    "electron-builder": "^22.10.5"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/prawin12345/konjugation.git"
  },
  "dependencies": {
    "electron-updater": "^4.3.8",
    "jquery": "^3.6.0"
  }
}
